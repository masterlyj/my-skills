# Fixture Policy

`run_eval.py` stores compact fixture definitions in code and writes them to a temporary directory at runtime. Keep fixtures small, deterministic, and focused on one rule at a time.

Use this directory for larger fixture files only when inline definitions become hard to read.
