# 默认：仓库内统一 LF，检出时按平台转换
* text=auto eol=lf

# PowerShell 脚本在 Windows 上用 CRLF，避免某些环境解析异常
*.ps1 text eol=crlf

# 二进制文件不做换行符转换
*.png binary
*.jpg binary
*.gif binary
*.pdf binary
*.woff binary
*.woff2 binary
